export type PricingConfig = {
  standard: number;
  optimizeBasic: number;
  hdSurcharge: number;
};

export const PRICING: PricingConfig = {
  standard: 200,
  optimizeBasic: 50,
  hdSurcharge: 200,
};
